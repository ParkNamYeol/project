<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.my01_helloworld">

    <application
        android:allowBackup="true"
        android:icon="@drawable/iu"
        android:label="@string/app_name"
        android:roundIcon="@drawable/iu"
        android:supportsRtl="true"
        android:theme="@style/Theme.My01_HelloWorld">
        <activity android:name=".Sub1Activity"></activity>
        <activity android:name=".MainActivity"> <!-- 처음에 띄워지는 창 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>